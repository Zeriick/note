# Intro

渐进上界符号 $O$ 存在c
$f(n) = O(g(n)) =\{ 0 <=f(n) <=cg(n) \}$
渐进下界符号 $\ohm$ 存在c
$f(n) = \ohm(g(n)) =\{0\leq cg(n)\leq f(n)\}$
紧渐近界符号 $\Theta$
非紧上界符号 $o$（高阶无穷小）
非紧下界符号 $\omega$  (高阶无穷大)

## 递归与分治

## main theorem
用主定理来分析分治的复杂度
$$T(n) = aT\left( \frac{n}{b} \right)+f(n)$$
+ case1 $$f(n)=O(n^{\log_{b}(a)-\epsilon})$$
	这是最常见的情况，合并的代价较小，则
	$$T(n)=\Theta(n^{\log_{b}a}) $$
+ case2 $$f(n) = \Theta(n^{\log_{b}a}\log^{\epsilon}n)$$
  即合并的代价在指数上同阶，例如二分查找就是这种情况，$log_{b}a =\log_{2}1=0$
	$$T(n)=\Theta(n^{\log_{b}a}\log^{\epsilon+1}n)$$
## 排列问题
```cpp
void Perm(int[] list,int k,int m){
	if (k == m) print;
	for(int i=k;i<=m;i++){
		swap(list[k], list[i]);
		Perm(list, k+1, m);
		swap(list[k], list[i]);
	}
}
```
## 整数划分问题
将正整数拆成一系列正整数之和
将最大加数不大于 m 的划分个数记作 $q(n,m)$。
可以建立 $q(n,m)$ 的如下递归关系：
$q(n,1) = 1$
$n < m,q(n,m) =q(n,n)$
$q(n,n)=1+q(n,n+1)$
$q(n,m)=q(n,m-1)+q(n-m,m)$
## 4-peg Hanoi

$$T_{4}(n) = 2T_{4}(n-k)+T_{3()}$$
$$k = \max \sum_{i=1}^{k} i \leq n$$
## bfptr

## 最接近点对
一维/二维 分治 合并
二维合并：先递归求出分治的最小值，再根据最小值合并

# dp
## 矩阵连乘
令矩阵 $i$ 的维数为 $p_{i-1}\times p_{i}$ 
$$m[i,j]=\begin{cases}
0   \;\;\;\;i=j  \\
\min_{i\leq k<j}\{m[i,k]+m[k+1,j]+p_{i-1}p_{k}p_{j}\}\;\;\;i<j
\end{cases}$$
## 最长公共子序列（LCS）
$$c[i][j] =\begin{cases}
0 \;\;\; & i,j=0 \\
c[i-1][j-1] + 1 \;\;\; & x_{i} =y_{j} \\
\max({c[i][j-1],c[i-1][j]})\;\;\;&x_{i}\neq y_{j}
\end{cases}$$
## 多边形最优三角剖分
$$t[i][j]=\begin{cases}
0\;\;\;\;&i=j  \\
\min_{i\leq k<j}\{t[i][k]+t[k+1][j]+w(v_{i-1}v_{k}v_{j})\}\;\;\;&i<j
\end{cases}$$
## 图像压缩
分段使得每一段中所有数不超过 $2^n$
设最优分段结果中第i段长度$l[i]$，该段$n=b[i]$ 
要求每段长度不大于256，所以事实上是一个大常数的线性dp
段信息3+8bit
$$s[i]=\min_{1\leq k\leq\min\{i,256\}}\{s[i-k]+k*b\max(i-k+1,i)\}+11$$
## 电路板布线
要求线不交错 第i个点的对应另一端为$\pi(i)$
$$S(i,j)=\begin{cases}
S(i-1,j)\;\;\;&j<\pi(i) \\
\max({S(i-1,j)},S(i-1,\pi(i)-1)+1)\;\;\;&j\geq \pi(i)
\end{cases}$$
## 流水线调度
两个机器，所有任务只能先a再b
![[Pasted image 20240511212552.png]]
## 背包
TODO：跳跃点优化

## 贪心

这章不用看

# 回溯
具有限界函数的深度优先生成法称为回溯法
记住基本的排列树和子集树遍历方法
### 子集树
```cpp
void bt(int t){
	if(t>n){
		output;
	}
	else{
		for(int i = 0;i<=1;i++){
			x[t] = i;
			bt(t+1);	
		}
	}
}
```
### 排列树
```cpp
void bt(int t){
	if(t>n){
		output;
	}
	else{
		for(int i = t;i<=n;i++){
			swap(x[t],x[i]);
			bt(t+1);	
			swap(x[t],x[i]);
		}
	}
}
```
## 连续邮资问题
假设国家发行了 n 种不同面值的邮票，并且规定每张信封上最多只允许贴 m 张邮票。连续邮资问题要求对于给定的 n 和 m 的值，给出邮票面值的最佳设计，在 1 张信封上可贴出从邮资 1 开始，增量为 1 的最大连续邮资区间。

解向量 $n$ 元组 $x[1:n]$ 表示n种不同的面值，如何快速计算其最大区间：
考察用这些邮票贴出k的所需数目，线性递推


通过前面具体实例的讨论容易看出，回溯算法的效率在很大程度上依赖于以下因素：
+ 产生 $x[k]$ 的时间；
+ 满足显约束的 $x[k]$ 值的个数；
+ 计算约束函数 constraint 的时间；
+ 计算上界函数 bound 的时间；
+ 满足约束函数和上界函数约束的所有 $x[k]$ 的个数。

# 分支界限法
分支限界法常以 广度优先 或以 最小耗费（最大效益）优先的方式搜索问题的解空间树。
分支界限法bfs子集树的方法是简单的
对于排列树，需要在节点中记录目前的排列现状，例如通过列表记录需要进一步搜索的顶点；或者存储当前的排列树。
以第一种方式为例 我们在节点中记录当前的排列
```cpp
struct Node{
	int s;     // 当前已确定的排列长度
	int x[n];  // 排列
}

prevnode = q.top();
for(int i = prevnode.s+1;i<=n;i++){
	Node newnode{s+1,new int[n+1]};
	memcpy(prevnode.x,newnode.x,sizeof(prevnode.x));
	swap(newnode.x[newnode.s],newnode.x[i]);
	q.push(newnode);
}
```
# NP
P 是在多项式时间内使用确定性算法可 解的所有判定问题的集合 NP 是在多项式时间内使用非确性算法可解的所有判定问题的集合 。

P 类问题是可以在多项式时间内解决并验证的一类问题 NP 类问题是可以多项式时间验证但是 不确定 能否在多项式时间内解决的一类问题 。

SAT：k个布尔变量构成的m个布尔表达式均可满足 是 NPC

SAT ->CNF-SAT 把任意个布尔表达式转换为合取式，对$E_{1}+E_{2}$的形式进行递归处理
![[Pasted image 20240512000258.png]]
这样每次递归都缩短了长度

CNF-SAT-> 3-SAT
把CNF中长度大于3的字句拆成3即可
![[Pasted image 20240512001041.png]]

3-SAT->CLIQUE
把每个子句都写成多个（最多三个）点，子句之间不矛盾的点之间连边，存在n团则满足3sat

CLIQUE->VERTEX-COVER
图存在k团则补图存在$|V|-k$ 顶点覆盖

VERTEX-COVER-> SUBSET-SUM
因为一条边对应的权值被三个数持有：两个点和一个边，为了确保三个都加上也不进位，所以最少构造成四进制.为了区分点和边，构造$|E|+1$ 位四进制数，点的首位置1，对每条边代表的位，把该条边和起点终点的该位置1。
![[Pasted image 20240512005720.png]]
则满足subset = t 意味着满足顶点覆盖。
因为t唯一确定了顶点个数，以及k的每位权值都为2，要么两个顶点属于k，要么一个顶点和一个边属于k，这也是VERTEX-COVER的定义。

VERTEX-COVER->HAM-CYCLE
